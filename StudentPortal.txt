// Student data structure
// used for storing basic student info for now, will be updated later

typedef struct {
    int id;
    char name[50];
    int age;        // student's age
    float cgpa;     // cumulative GPA
} Student;
